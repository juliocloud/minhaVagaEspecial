import React from 'react';
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
     container: {
          backgroundColor: '#000',
          width: 90,
          height: 40,
          borderRadius: 16,
          textAlign: 'center',
     },
     title: {
          textAlign: 'center',
          marginTop: 'auto',
          marginBottom: 'auto',
          color: '#FFF',
          fontWeight: 'bold',
          fontSize: 17
     }
})